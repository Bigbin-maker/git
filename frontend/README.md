# AI-MBSE-Demo Frontend

React + Vite + TypeScript 前端，使用 Ant Design 和 ReactFlow 展示 MBSE 演示流程。

```bash
npm install
npm run dev
```

默认后端地址：

```text
http://localhost:8000/api
```

可通过 `VITE_API_BASE_URL` 覆盖。
