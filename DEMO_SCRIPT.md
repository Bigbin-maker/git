# AI-MBSE-Demo 现场演示脚本

## 1. 首页查看系统状态

打开 `http://localhost:5173`。

讲解话术：当前系统已经接入 LLM、SysML API 适配器和本地设计数据库。外部接口不可用时会自动降级为 Mock，保证演示流程不中断。仿真工具尚未安装，因此仿真模块当前展示接口预留能力。

预期看到：LLM、SysML、Simulation、SQLite 状态，以及 5 个演示步骤卡片。

## 2. 加载演示需求

进入“需求分析”，点击“加载宽带通信演示数据”，再点击“开始需求分析”。

预期看到：生成 5 条结构化需求：

- REQ-001 用户接入能力
- REQ-002 链路资源管理能力
- REQ-003 业务调度能力
- REQ-004 网络状态监测能力
- REQ-005 故障告警能力

讲解话术：AI 将自然语言需求转换为可追溯的结构化条目，并给出规范性评分和补充建议。

## 3. 人在回路确认

在需求表点击“全部确认”，或逐条点击“确认 / 修改 / 拒绝”。

预期看到：需求状态从“待确认”变成“已确认”。

讲解话术：AI 只生成候选结果，正式进入模型链路前需要人工确认，满足人在回路要求。

## 4. 生成 SysML 模型

进入“模型生成”，选择已确认需求，点击“生成 SysML 模型”。

预期看到：候选 Requirement、UseCase、Block、Interface、Activity，以及 ReactFlow 追溯关系图。

讲解话术：系统根据确认需求自动生成候选模型元素，并保留来源需求和追溯关系。

## 5. 提交到 SysML

点击“提交到 SysML”。

预期看到：如果真实 API 可用，提示已通过 SysML API 创建；如果不可用，提示已切换 Mock 并本地保存成功。

讲解话术：这里展示的是适配器模式，真实 SysML API 和本地 Mock 共用同一业务接口。

## 6. 变更影响分析

进入“变更影响分析”，选择 `REQ-001 用户接入能力`，输入“用户接入能力需要支持更高并发终端数量。”，点击“开始影响分析”。

预期看到：直接影响用户接入 UseCase，间接影响宽带通信系统 Block、用户接入接口 Interface，并显示风险等级和影响路径。

讲解话术：系统基于模型追溯图做 BFS 传播，帮助评审变更会影响哪些设计元素。

## 7. 模型预评审

进入“模型预评审”，点击“开始模型预评审”。

预期看到：模型评分、评审结论、问题列表、统计信息，并可导出 JSON 报告。

讲解话术：预评审规则会检查追溯关系、用例分配、接口关联、命名唯一性和人工确认状态。

## 8. 仿真 Mock

进入“仿真 Mock”，选择 `BLK-001 宽带通信系统`，点击“运行仿真 Mock”。

预期看到：

- 时延：120 ms
- 吞吐量：50 Mbps
- 可用性：99.9%
- 风险等级：低

讲解话术：仿真工具暂未安装，当前模块展示未来接入真实仿真工具的接口位置和结果呈现方式。
